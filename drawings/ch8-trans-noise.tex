\documentclass[12pt]{article}

\usepackage{../thesis}

\usepackage{tikz} 
\usepackage{pgfplots} % drawing plots right here in this file!
\pgfplotsset{compat=1.9} % latest stable release

\usepgfplotslibrary{groupplots}

\pgfplotsset{
  every axis/.append style={
    scale only axis,
    %width=2.5in,height=1.6in,
  },
  /tikz/every picture/.append style={
    baseline
  }
}
\pgfplotscreateplotcyclelist{my color list}{%
    blue,red,black,yellow,brown,teal,orange,violet,cyan,}

\pgfplotscreateplotcyclelist{color list doubled}{%
    blue,blue,red,red,black,black,yellow,yellow,brown,brown,teal,teal,orange,orange,violet,violet,cyan,cyan,}

\begin{document}

\pagestyle{empty}
\begin{tabular}{r r}

\begin{tikzpicture}

\begin{groupplot}[
    group style={group size=1 by 4,
                 vertical sep=0.1in,
                 horizontal sep=0.5in,
                 x descriptions at=edge bottom,
                 },
    width=2.25in,height=1.6in,
    xmode=log,ymode=log,
    xmin=1,xmax=2e4,
    ymin=3e-11,ymax=2e-8,
    ylabel={$S_I$ (\Inoise{})},
    xlabel={Freqency (\si{\hertz})}
    ]

  \nextgroupplot[cycle list name=my color list]

    \foreach \yindex in {1,...,8} {
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-r29.dat};
    }

  \nextgroupplot[cycle list name=my color list]

    \foreach \yindex in {1,...,8} {
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-r30.dat};
    }

  \nextgroupplot[cycle list name=my color list]

    \foreach \yindex in {1,...,8} {
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-r31.dat};
    }
    
  \nextgroupplot[cycle list name=my color list]

    \foreach \yindex in {1,...,8} {
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-r32.dat};
    }

\end{groupplot}

\end{tikzpicture}

  &

\begin{tikzpicture}

\begin{groupplot}[
    group style={group size=1 by 4,
                 vertical sep=0.1in,
                 horizontal sep=0.5in,
                 x descriptions at=edge bottom,
                 },
    width=2.25in,height=1.6in,
    xmode=log,ymode=log,
    xmin=1,xmax=2e4,
    ymin=1e-16,ymax=2e-14,
    ylabel={$S_{P_{bolo}}$ (\Pnoise{})},
    xlabel={Freqency (\si{\hertz}),}
    ]

  \nextgroupplot[cycle list name=color list doubled]

    \foreach \yindex in {9,...,16} {
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-r29.dat};
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-model-r29.dat};
    }

  \nextgroupplot[cycle list name=color list doubled]
  
    \foreach \yindex in {9,...,16} {
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-r30.dat};
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-model-r30.dat};
    }

  \nextgroupplot[cycle list name=color list doubled]
  
    \foreach \yindex in {9,...,16} {
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-r31.dat};
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-model-r31.dat};
    }

  \nextgroupplot[cycle list name=color list doubled]
  
    \foreach \yindex in {9,...,16} {
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-r32.dat};
	  \addplot table[x=f,y index=\yindex]{../data/ch8-bias-step-noise-model-r32.dat};
    }

\end{groupplot}

\end{tikzpicture}
\end{tabular}
\end{document}
