\documentclass[12pt]{article}

\usepackage{../thesis}

\usepackage{tikz} 
\usepackage{pgfplots} % drawing plots right here in this file!
\pgfplotsset{compat=1.9} % latest stable release

\pgfplotsset{
  every axis/.append style={
    scale only axis,
    width=2.3in,
  },
  /tikz/every picture/.append style={
    baseline
  }
}

\begin{document}

\pagestyle{empty}

\begin{tabular}{rr}

  \begin{tikzpicture}[trim axis left]
	\begin{axis}[
        xmin=5, xmax=20,ymin=0,
		title={Required $\tau_{eff}$ vs Video Frame Rate},
		xlabel={Video Frame Rate (\si{\s^{-1}})}, ylabel={Required $\tau_{eff}$ (\si{ms})} ]
	  \addplot+[mark=none] table[x=fps,y=teff_reqd_2] {../data/ch5-teff-reqd.dat};
	\end{axis}
  \end{tikzpicture}

  &

  \begin{tikzpicture}[trim axis right]
	\begin{axis}[xmin=-2,xmax=10,ymin=0,
		title={Impact of \TES\ timestream filter},
		xlabel={Time (arb. units)}, ylabel={Response (arb. units)} ]

	  \addplot+[mark=none] table[x=tp,y=bump] {../data/ch5-bumps.dat};
	  \addlegendentry{Unfiltered}
	  \addplot+[mark=none] table[x=tp,y=bump_conv] {../data/ch5-bumps.dat};
	  \addlegendentry{Filtered}

	\end{axis}
  \end{tikzpicture}
  \\
\end{tabular}

\end{document}
