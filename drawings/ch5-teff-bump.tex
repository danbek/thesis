\documentclass[12pt]{article}

\usepackage{../thesis}

\usepackage{tikz} 
\usepackage{pgfplots} % drawing plots right here in this file!
\pgfplotsset{compat=1.9} % latest stable release

\pgfplotsset{
  every axis/.append style={
    scale only axis,
    width=2.3in,
  },
  /tikz/every picture/.append style={
    baseline
  }
}

\begin{document}

\pagestyle{empty}

  \begin{tikzpicture}[trim axis left]
	\begin{loglogaxis}[
        xmin=1e-2, xmax=3,
        legend pos=south east,
		title={Required $\tau_{eff}$ vs Blurring Factor},
		xlabel={$(q_{filt} - q_{raw})/q_{raw}$}, ylabel={Required $\tau_{eff}$ (\si{ms})} ]
	  \addplot+[mark=none] table[x=fwhm_over,y=teff6] {../data/ch5-teff-reqd.dat};
      \addlegendentry{6 FPS}
	  \addplot+[mark=none] table[x=fwhm_over,y=teff10] {../data/ch5-teff-reqd.dat};
      \addlegendentry{10 FPS}
	  \addplot+[mark=none] table[x=fwhm_over,y=teff20] {../data/ch5-teff-reqd.dat};
      \addlegendentry{20 FPS}
	\end{loglogaxis}
  \end{tikzpicture}


\end{document}
