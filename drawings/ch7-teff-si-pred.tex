\documentclass[12pt]{article}

\usepackage{../thesis}

\usepackage{tikz} 
\usepackage{pgfplots} % drawing plots right here in this file!
\pgfplotsset{compat=1.9} % latest stable release

\pgfplotsset{
  every axis/.append style={
    scale only axis,
    width=2.5in,
  },
  /tikz/every picture/.append style={
    baseline
  }
}

\begin{document}

\pagestyle{empty}

\begin{tabular}{rr}

  \begin{tikzpicture}[trim axis left]
	\begin{axis}[xmin=0,ymin=0,
		title={Predicted $\tau_{eff}$},
		xlabel={$\tau_{eff}$ (\si{\ms})}, ylabel={Quantity} ]
	  \addplot [hist={data min=0,data max=6,bins=20},
		        fill=blue!75]
	    table[y=teff_pred]{../data/ch7-all-loop.dat};
	\end{axis}
  \end{tikzpicture}

  &

  \begin{tikzpicture}[trim axis right]
	\begin{axis}[xmin=0.25,ymin=0,
		title={Predicted $s_I(0)$},
		xlabel={$s_I(0)$ (\si{\per \uV})}, ylabel={Quantity} ]
	  \addplot [hist={data min=0.25,data max=1,bins=20},
		        fill=blue!75]
	    table[y=sI_pred]{../data/ch7-all-loop.dat};
	\end{axis}
  \end{tikzpicture}

\end{tabular}

\end{document}
